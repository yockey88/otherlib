/entry: main

$main:
  set r0, data.load-text
  set r1, data.string-len
  set r2, 3 ; warning level
  syscall core.log
  ; mov r1, r2
  ; dump r1
  ; dump r2
  ; dump r3
end

#data {
  ; address to write load text to in memory
  .string-len: int32 = 3
  .load-text: string = "abc"
}