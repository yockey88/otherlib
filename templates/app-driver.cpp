/**
 * \file ${project-name}_driver.cpp
 **/
#include "${project-name}_driver.hpp"

namespace ${project-name} {

  void ${project-name}_driver::on_initialize(const command_line& cmd) {
    // initialize driver, parse command line args, prepare to run
  }

  void ${project-name}_driver::run() {
    // main body of your application
  }

  void ${project-name}_driver::on_shutdown() {
    // clean up resources, save state if needed
  }
  
} // namespace ${project-name}